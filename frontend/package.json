{
  "name": "phishguard-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only tooling for the PhishGuard static frontend (vitest + eslint). The deployed frontend/ and docs/ stay plain static files.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "vitest": "^3.0.0"
  }
}
